
class Human extends Entity
	static function sayHello()
	end
end
local h = new Human(1 + 2);
print(h.name .. "\n");
h:sayHello()

enum begin end
enum Suit begin end
enum class colours begin end

switch 3 do
	default:
	case 3: break
end

function f(a: ?string = "Hello"): void end

h?.name ?? "John"
a ? b : c
h?["Hello"]

goto label;
::label::
